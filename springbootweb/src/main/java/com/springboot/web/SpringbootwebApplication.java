package com.springboot.web;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringbootwebApplication {

	public static void main(String[] args) {
		SpringApplication.run(SpringbootwebApplication.class, args);
	
	//  in this Springboot project we will see Jsp view
		// there is only 2 step 
		// 1.create jsp page as per controller methods
		// 2.add prefix and suffix 
		
		//prefix are those folders inside which jsp page occurs
		//suffix are extension of files
	
	}

}
